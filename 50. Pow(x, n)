class Solution {
public:
    // Function to calculate x raised to the power n
    double myPow(double x, int n) {
        // Directly using the built-in pow function to compute x^n
        return pow(x, n); // Return the result of x raised to the power of n
    }
};
